import 'package:flutter/material.dart';

class CardBio extends StatelessWidget {
  const CardBio({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
